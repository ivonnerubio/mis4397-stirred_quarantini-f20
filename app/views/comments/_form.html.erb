<%= form_for([ @blog, Comment.new], remote: true) do |form| %>
    <%= form.text_area :body, class: 'form-control', required: true  %>
    <%= form.submit "Comment", class: 'btn btn-danger btn-sm btn-block' %>
<% end %>
