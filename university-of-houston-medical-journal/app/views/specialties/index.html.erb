<p id="notice"><%= notice %></p>

<h1>Specialties</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>String</th>
      <th>Category title</th>
      <th>String</th>
      <th>Category description</th>
      <th>Text</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @specialties.each do |specialty| %>
      <tr>
        <td><%= specialty.title %></td>
        <td><%= specialty.string %></td>
        <td><%= specialty.category_title %></td>
        <td><%= specialty.string %></td>
        <td><%= specialty.category_description %></td>
        <td><%= specialty.text %></td>
        <td><%= link_to 'Show', specialty %></td>
        <td><%= link_to 'Edit', edit_specialty_path(specialty) %></td>
        <td><%= link_to 'Destroy', specialty, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Specialty', new_specialty_path %>
